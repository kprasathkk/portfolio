# React + Vite

This project is about the portfolio built using React , Nodejs.

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

To execute in local follow the below steps

Pull the main branch to your local

run the command "npm run dev" to execute in local.

By default it will run http://localhost:5713
